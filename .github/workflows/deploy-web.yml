name: Deploy Web

on:
  push:
    branches: [main]
    paths:
      - "web/**"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger EasyPanel deploy
        run: curl -fsS -X GET "${{ secrets.EASYPANEL_DEPLOY_WEB }}"
